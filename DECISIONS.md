# 3DPrintPosters - Decisions

Last updated: 2026-07-12

## Purpose

This file is the durable decision log for product and architecture choices that should guide future AI developers. Keep current implementation status, active focus, and open risks in `PROJECT_STATE.md`. Keep operating rules, local commands, security rules, and verification commands in `AGENTS.md`. Keep chronology in `CHANGELOG.md`, the front-end design system (brand, tokens, type, landing/hero) in `docs/DESIGN.md`, detailed service contracts in `docs/`, and Meshy implementation detail in `research/MESHY_SERVICE_IMPLEMENTATION_PLAN.md`.

Do not let this file become a task list, experiment log, or second changelog. Add a decision here only when future work should treat it as policy unless a later review explicitly reverses it.

## Durable Decisions

- Keep print-file/model generation server-side. Do not move geometry generation, provider API keys, texture packaging, or fulfillment logic into the browser.
- Keep `services/print-file-generator` as the production print-file boundary. Do not vendor the standalone `E:\PROJECTS\print-file-generator` Flask routes, SQLite state, browser session handling, local CLI flow, TD1 hardware code, or old open-surface mesh topology.
- Direct Vertex/Gemini remains the MVP proof-generation path. Cloudflare AI Gateway is deferred until provider comparison, centralized observability, rate limits, or retries matter.
- Proof/style prompt controls belong in server-read workflow configuration, not in browser-only state. The `/admin` page edits that config only for accounts with the Firebase Auth admin custom claim; this role gate must stay server-enforced, not browser-only.
- Admin reference images for 2D proofs are per-style art direction, not customer identity input. When selected style reference images are present, they take priority for overall visual style, render style, character design language, stylization level, outfit, costume, colors, body styling, proportions, materials, finish, pose language, and accessories; the customer source photo supplies face identity only. The 3D provider still receives the approved 2D proof rather than separate admin prompt images.
- The five-experiment heightmap cycle is complete. Full image-to-3D reconstruction providers such as TripoSR, Stable Fast 3D, TRELLIS, SAM 3D Objects, TriplaneGaussian, and Meshy-style providers are rejected only for poster relief because they reconstruct standalone objects rather than image-plane depth. They are now valid candidates for the standalone figurine product direction.
- Meshy.ai was the first provider evaluated for the figurine direction and remains the Creative Lab provider plus direct-style rollback option. Keep every generated-3D provider behind a replaceable server-side adapter, download provider assets into our Storage quickly because external result retention may be short, and record provider/model/version/credits/cost metadata without storing secrets.
- Hi3D (Hitem3D v2.1 via `api.hitem3d.ai`) is the decided production provider for direct Multi-Image-to-3D figurine styles as of 2026-07-08; Elliot validated its output quality manually and the API was live-validated the same day (25 credits ≈ $0.50 and ~7 minutes per generation, GLB canonical output, result URLs expire after 1 hour so assets download into our Storage immediately). Meshy remains the Creative Lab provider and the selectable rollback for direct styles. Per-style provider/model selection is server-validated workflow config (`directMultiImageProviderCatalog`) editable in `/admin`; the 3D AI Studio aggregator was evaluated and rejected because its API does not expose Hitem3D, and Hugging Face only hosts Sparc3D demos, not production Hitem3D weights (see `.tmp` spike notes summarized in `research/FIGURINE_PROVIDER_RESEARCH.md`).
- Corrected figurine base architecture: do not ask Vertex/Gemini, Meshy, Hi3D, or another generated-3D provider to generate customer name text, star details, or the final reusable product base. Provider calls should generate the figurine/body object; a saved base STL asset, deterministic name-on-base geometry, and deterministic body/base assembly belong in `services/print-file-generator`. As of 2026-06-03, `printu-round-v1` is the approved reusable base asset at `services/print-file-generator/assets/figurine-bases/printu-round-v1/`; it is a clean unpersonalized sliced-round base with a flat front name face and local watertight/winding verification. The `previews/elliott/` sample captures the approved default raised-text placement: smaller, lower, centered in the flat front rectangle, and partially embedded into the structure. The paid Experiment 004 thumbnail included a base because the upstream 2D/reference path allowed one; future concept and generated-3D prompts should explicitly request no base/pedestal/platform unless a base experiment deliberately passes `--base-label`.
- Current figurine print target is about 6 inches / `150mm` tall. Treat raw Creative Lab GLB units as provider units until the deterministic assembly service applies an explicit target-height scale; for the validated `f604d393-bfa2-4779-b05b-f6a2082604c9` sample, `scale = 150 / 1.899262249 = 78.978034802`. Do not bake Blender review-scene `1000x` display scale into reusable assets.
- Future Meshy experiments should use the single standard runner `scripts/meshy/run-standard-figurine-experiment.mjs` / `npm run meshy:experiment -- -- --experiment-slug <slug>`. It supports both the standard body-only Vertex/Gemini -> Meshy multi-view -> Meshy 3D path and the Creative Lab Figure workflow. As of Experiment 009, Creative Lab Figure raw GLB generation is the approved upstream figure step for the first product path; downstream print conversion/repair is a separate validation stage. Legacy Meshy runners are archived under `scripts/meshy/archive/2026-05-26-legacy-runners/` for historical reproduction only.
- Public customer creation requires an email/password account before upload, job creation, proof approval, scene rendering, base naming, or checkout. Anonymous or guest sessions are not a public creation path. Verified email remains a production hardening step once password reset/email lifecycle flows are ready.
- The user model has three first-class surfaces: customer accounts, admin/operator users, and print-partner users. Admin and operator capabilities are server-enforced by Firebase Auth custom claims, not only hidden in the browser. Print partners remain future scoped users with access only to assigned approved print packages, not broad customer account or Storage access.
- Customer-facing creation credits are the public quota model. Provider spend steps must check, reserve, consume, refund, or adjust credits server-side through an auditable ledger, and the ledger should tie back to `jobCost` so support/admin views can compare customer credits, actual provider cost, partner cost, refunds, and margin.
- The intended first public purchase path is automated checkout for validated full-color partner fulfillment. Checkout remains blocked until the selected partner's file format, quote, order, policy, shipping, and print-readiness requirements are represented honestly in backend eligibility state.
- Deterministic brightness-to-height providers (`posterized_luminance`, `continuous_luminance`, `lithophane_baseline`) are reference providers, not the default checkout path.
- The chosen relief provider is `masked_depth_detail_blend`: 768px geometry-analysis cleanup, Depth Anything V2 semantic depth for broad shape/background control, contour-smoothed SegFormer subject masking, a HueForge-leaning `lithophane_baseline` subject height blend, guided-filter bas-relief compression, reduced detail-preserving face smoothing, face/forehead pit guarding, and the existing closed STL/GLB generator.
- Portrait relief tuning is currently face-aware inside server-side print-file generation. Local OpenCV Haar face boxes build soft face-oval, central-face, eye, nose, and mouth masks for relief tuning and debug visibility only; defer an external face API fallback until local detection misses real product-flow cases. Do not reintroduce the removed nose-specific height boost unless human review explicitly reverses that decision.
- Current relief-quality direction is surface-intent aware generation: the hybrid provider now infers v1 smooth/crisp/emboss/texture masks so skin, scalp, neck, ears, hands, simple clothing, and backgrounds remain smooth by default, crisp text/logos/graphic edges get a separate raised emboss treatment, and shallow material texture is enabled only from explicit proof-generation or human override metadata. `surface_intent_status.roughness_metrics` reports whether smooth subject/background regions are still too noisy or graphic regions are too flat.
- The recommended maturity path now has two tracks: (1) customer-facing figurine demand proof using API-backed image-to-3D providers, starting with Meshy; (2) parked poster-relief R&D using API-backed proof generation, monocular depth, subject segmentation, and deterministic server-side print-file generation.
- The current job page is the first quality-control surface: approved proof, generated heightmap, interactive GLB preview, printability status, and warnings. Local Functions emulator runs mirror the full print-file bundle under `.tmp/print-files/{uid}/{jobId}` instead of exposing customer-facing artifact download links.
- The public-facing brand is **3DPrintU** with canonical domain `3dprintu.com`. `/` is the marketing landing page and `/start` is the upload/creation flow (PWA `start_url` = `/start`). This is a brand-surface decision only: the repo name, Firebase project id, env keys, Cloud Storage bucket, and all source-code identifiers stay as-is, so internal references to "posters"/"3dprintyou" are not a bug. The landing hero uses a committed scroll-scrubbed WebP frame sequence (no runtime video transcode) and the warm Fraunces/Inter token system in `globals.css`; existing app pages get the tokens and a light heading/button retouch, not a redesign. Production DNS, brand icons/OG art, a bespoke wordmark, and real gallery photography are tracked follow-ups, not part of this decision. The full design system (color/type tokens, landing-page architecture, hero scrub spec, and design rationale) lives in `docs/DESIGN.md`.
- Public SEO launch update, 2026-07-05: `https://3dprintyou.com` is the canonical coming-soon marketing domain for customer acquisition, superseding the earlier `3dprintu.com` public-DNS follow-up for now. It is implemented as a standalone static Next/Railway site in `E:\PROJECTS\3DPrintyou` / `KielRN/3dprintyou` and is live at the apex through Cloudflare/Railway. Keep the full product app, Functions, Firebase data, and print-file services in this repo until the app is ready for a separate staging/production deployment. Keep `api.3dprintyou.com` reserved for the existing Meshy webhook Worker. `www.3dprintyou.com` is not canonical; DNS exists, but HTTP returned `404` on 2026-07-05, so finish the Railway custom domain or redirect before using it publicly.
- The figurine product has two approved style families as of 2026-07-03, and they use different provider pipelines by design. (1) **Chibi**: Meshy Creative Lab Figure prototype/build, approved from Experiment 011; Creative Lab chibi-fies its input, so it serves the chibi/toy style. Current Chibi inputs are either template face swaps or realistic-person cleanup renders, depending on the style doc. (2) **Faithful/realistic identity**: direct Multi-Image-to-3D (`--skip-multiview` in the standard runner, committed `867c0b9`); the production recipe from Experiment 019 is a single Nano Banana Pro 2K identity-locked front image submitted as one `image_urls` input, with Hi3D now the decided direct provider. Do not treat Multi-Image-to-3D as merely a fallback for Creative Lab failures — the two paths serve different styles. Extra side views and single-canvas turnaround sheets are rejected for the faithful style.
- Per-style 3D workflow selection is explicit as of 2026-07-05. Workflow styles carry `generationWorkflow`: `creative_lab_figure` routes approved proofs through Meshy Creative Lab API, while `direct_multi_image_to_3d` routes the approved image to the selected direct generated-3D provider. Legacy styles without the field default to Creative Lab so existing Chibi behavior is preserved.
- The Chibi template customer flows are Creative Lab concept-gate styles. `chibi_figure` and `chibi_female` use `proofMode: "template_face_swap"` plus `generationWorkflow: "creative_lab_figure"`: Vertex/Gemini creates one face-swapped identity image from the customer photo and the first enabled template, Meshy Creative Lab prototype creates the single customer-reviewed concept, approval records that concept, and the post-payment build continues from the stored prototype task. Original template characters require IP/likeness policy clearance before public traffic. The current compact source of truth is `docs/Workflows/figurine-style-workflow-contracts.md`.
- The figurine funnel is a four-page storyfront as of 2026-07-10, and its routing plus honesty rules are policy. Routes: `/start` (style gallery; PWA `start_url` and landing CTAs keep pointing here) → `/start/[styleId]` (per-style project page; unknown/disabled ids redirect to `/start`) → `/jobs/[jobId]` (concept reveal) → `/jobs/[jobId]/home` (scene + claim; direct visits without an approved concept redirect to the job page). Customer surfaces never show figurine GLBs, base GLBs, print-readiness, or `figurineBuild` internals — those are operator-only; the customer sees the 2D concept, and checkout lives on page 4, unlocked at concept approval. Honest psychology only (plan §5/§5a): second-person voice, victory framing, perceptual sculpting through lighting/composition; no fabricated counters, reviews, scarcity, or guarantee copy; no prices shown until a real price source exists ("Final price at checkout."); the page-4 scene render is an honestly-captioned artist's visualization, is hard-capped server-side, and may never gate or delay checkout. Pre-payment base naming records the sign and generates the named base but defers body/base assembly to the post-payment build (operator print-readiness tooling), since the figurine body no longer exists before payment.
- The photo-driven Chibi customer flows are the public `Chibi male` / `Chibi female` Creative Lab styles as of 2026-07-09. `chibi_photo_male` and `chibi_photo_female` use `proofMode: "generated_options"` plus `proofRendering: "realistic_person"` and `generationWorkflow: "creative_lab_figure"`: Vertex/Gemini creates one internal realistic full-body cleanup render from the customer photo, Meshy Creative Lab prototype performs the Chibi stylization and creates the single customer-reviewed concept, approval records that concept, and the post-payment build continues from the stored prototype task. The current compact source of truth is `docs/Workflows/figurine-style-workflow-contracts.md`.
- The Heroic fantasy customer flows are direct Multi-Image-to-3D on Hi3D by default as of 2026-07-08. `heroic_fantasy_male` and `heroic_fantasy_female` use `proofMode: "template_face_swap"` plus `generationWorkflow: "direct_multi_image_to_3d"`: Vertex/Gemini swaps the customer face into the enabled admin template, the swapped image is the single customer-reviewed proof/direct-3D input, approval records that concept, and the post-payment build sends that image to the job-stamped direct provider/model. This workflow intentionally skips the Creative Lab prototype gate; provider GLB, `printReadiness`, and build failures are operator/support concerns, not customer checkout gates. The current compact source of truth is `docs/Workflows/figurine-style-workflow-contracts.md`.
- The Super Hero customer flows follow the same direct Multi-Image-to-3D contract on Hi3D. `creative_lab_figure` is the historical/load-bearing male style ID now labeled `Super Hero Figure - Male`, and `super_hero_figure_female` is the female counterpart. The current compact source of truth is `docs/Workflows/figurine-style-workflow-contracts.md`.
- Nano Banana Pro (`gemini-3-pro-image-preview` on Vertex via the existing express `VERTEX_API_KEY`) is the identity-image generator for the faithful figurine style. Identity input images must be 2K — ~1K inputs measurably destroy the micro-detail Meshy needs (Experiment 015 vs 019a). Qwen-Image-Edit and FLUX.2 were evaluated and rejected for now because they are not serverless-reachable on Vertex or OpenRouter; HF Inference Providers (`HF_TOKEN`) is the designated future route if NB Pro plateaus.
- Funded-build inversion (2026-07-09, storyfront revamp §4b): figurine 3D asset generation (Meshy Creative Lab build / Hi3D direct — GLB/STL artifacts, `figurinePreview`) runs AFTER payment, not at approval. The flow is `approveGeneratedImage` (approval-only for figurines: stamps `approvedImagePath` + `checkoutEligibility: { eligible: true, reason: "concept_approved" }`, returns in seconds) → `createCheckoutSession` → Stripe `checkout.session.completed` webhook stamps `figurineBuild: { status: "queued", queuedAt, attempts: 0 }` (guarded: only when no `figurineBuild` record exists, so webhook redelivery can never re-queue a running/finished build) → `onFigurineBuildQueued` Firestore trigger transactionally claims `queued -> running` (the double-build guard) and runs `runFigurineBuild()` in `apps/functions/src/figurineBuild.ts` — the provider path extracted unchanged from the old approval flow. Concept-image generation (Vertex proofs/face-swap and the Meshy Creative Lab prototype concept inside `createGenerationJob`) stays pre-payment; poster approval still builds print files pre-payment. Money-then-fail is the worst path: funded build failures must always surface in `supportSummary` (open note) plus order `fulfillment.productionSubState: "build_failed"` and stay re-queueable via the admin `requeueFigurineBuild` callable, and they must never surface as customer-facing job failures — the customer keeps seeing their order in production while support handles it. Rationale: no ~$0.50 + ~7-minute provider spend on jobs that never convert, approval becomes instant for the customer, and funded → operator matches the disclosed T&C business process. Companion decision: customers never see the figurine GLB — 3D surfaces are operator-only, and the page-4 `generateScenePreview` Vertex scene render (bookshelf/desk plates under `admin/scene-plates/`, hard cap 2 renders per scene per job) is presentation garnish that must never gate or delay checkout.
- Manual studio review checkout is the only approved pay-before-proof exception as of 2026-07-12. It is offered only after the server marks a figurine generation terminal or the stale-generation reconciler times it out; a client timeout alone is not enough. The existing job remains the ledger, and fallback checkout uses `createFallbackFigurineCheckoutSession` with `fulfillmentMode: "manual_proof_required"` rather than relaxing `createCheckoutSession`. Paid manual-review orders open support/operator work and must not queue provider 3D generation until an operator supplies a reviewed concept path and runs `release_manual_proof`, which stamps `approvedImagePath` and queues the existing guarded `figurineBuild` trigger exactly once.
